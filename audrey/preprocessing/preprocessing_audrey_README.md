To run the pipeeline data should be stored as following, where `D` is the data directory

```
D/raw
D/processed
    /sorted  
        F1.raw
        F2.raw
        FN.raw

        /spikesortingexport  
```